services:
  - type: web
    name: agent-buddy-socketio
    env: node
    buildCommand: npm install
    startCommand: node socketio-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000 